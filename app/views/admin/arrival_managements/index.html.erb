<%= render 'sidebar'%>
<h1>入荷登録一覧</h1>

<div class="col-lg-8">
	<table class="table table-hover">
		<thead>
			<tr>
				<th>ユーザー名<%#= customer.id %></th>
				<th><%#= %></th>
				<th>商品名<%#= product_id</th>
				<th></th>
				<th>更新日時<%= crated_at %></th>
				<th></th>
			</tr>
		</thead>
		<tbody>
	   <% @arrivals.each do |arrival| %>
			<tr>
				<td><%= arrival.product_id %></td>
				<td><%= arrival.product.product_name %></td>
				<td><%= arrival.product.artist.artist_name %></td>
				<td><%= arrival.arrival_date %></td>
				<td><%= arrival.arrival_count %></td>
	　　　　</tr>
			<% end %>
		</tbody>
	</table>
</div>
<%= paginate @arrivals %>




