<h1>Public::Orders#create</h1>
<p>Find me in app/views/public/orders/create.html.erb</p>


<% current_customer.cartitems.each do |cart_item| %>
		     <tr>
	            <td><%= attachment_image_tag cart_item.product, :product_image, :fill, 250, 250, fallback: "no_image.png", size:'150x150' %>
				<td text-align:center;><font size="4"><%= cart_item.product.product_name %>
	                <br>
	                <br>アーティスト:<font size="3"><%= cart_item.product.artist.artist_name %></font>
	                <br>
	                <br >ジャンル:<font size="3"><%= cart_item.product.genre.genre_name %><br></font>
		        </td>
				 <td text-align:center;><font size="4"><%= cart_item.purchase_quantity %>個</font></td>
					<td text-align:center;><font size="4"><%= cart_item.product.product_price %>円</font></td>
		            <td text-align:center;><font size="4"><%= cart_item.product.product_price * cart_item.purchase_quantity %>円</font></td>
			        <td><%= link_to '削除', cart_item_path(cart_item.id), method: :delete %></td>
		　　　　     <td><%= form_for(cart_item, url:cart_item_path(cart_item)) do |f| %>
					<%= f.number_field :purchase_quantity, min:"1", max: 10%>
					    <%= f.submit '数量を変更' %>
				    <% end %>
				</td>
			</tr>
		<% end %>
	</table>





			<table class="table table-hover">
					<tr>
				        <th>商品画像</th>
						<th>商品名</th>
						<th>数量</th>
						<th>価格</th>
				        <th class="text-right">合計</th>
				    </tr>
			<tr>
	            <td><%= attachment_image_tag cart_item.product, :product_image, :fill, 250, 250, fallback: "no_image.png", size:'150x150' %>
				<td text-align:center;><font size="4"> <%= f.product.product_name %>
	                <br>
	                <br>金額:<font size="3"><%= cart_item.product.artist.artist_name %></font>
	                <br>
	                <br >ジャンル:<font size="3"><%= cart_item.product.genre.genre_name %><br></font>
		        </td>
				 <td text-align:center;><font size="4"><%= cart_item.purchase_quantity %>個</font></td>
					<td text-align:center;><font size="4"><%= cart_item.product.product_price %>円</font></td>
		            <td text-align:center;><font size="4"><%= cart_item.product.product_price * cart_item.purchase_quantity %>円</font></td>
			        <td><%= link_to '削除', cart_item_path(cart_item.id), method: :delete %></td>
		　　　　     <td><%= form_for(cart_item, url:cart_item_path(cart_item)) do |f| %>
					<%= f.number_field :purchase_quantity, min:"1", max: 10%>
					    <%= f.submit '数量を変更' %>
				    <% end %>
				</td>
			</tr>



<div class="main-visual">
   <h1 class="text-success">購入履歴一覧画面</h1>
   <% @orders.each do |o|  %>
	<table class="table table-hover col-lg-7 ">
		<thead>
			<tr>
				<th class="create_day">注文日</th>
				<th class="">配送状況</th>
				<th class=""></th>
			</tr>
		</thead>
			<tbody>
				<tr>
					<td class="order_day"><%= o.created_at.strftime("%Y年%m月%d %H:%M") %></td>
					<td class="delivery_status" ><%= o.delivery_status_i18n %></td>
					<td class=""><%= link_to '購入履歴詳細リンク', order_path(o.id) %></td>
				</tr>
				<% o.order_details.each do |f| %>
				   <tr class="table">
					　<td><%= attachment_image_tag f.product, :product_image, :fill, 250, 250, fallback: "no_image.png", size:'150x150' %></td>
					 <td><div class="product_font">商品名: <%= f.product.product_name %><br></div>
					 <div class="price_font">金額: <%= f.product.product_price %>円<br></div>
					 <div class="count_font">点数: <%= f. order_quantity %>点</td></div>
				　</tr>
				<% end %>
	       </tbody>
		</table>
	<% end %>
 <%= paginate @orders %>
</div>














		<div class="col-lg-10">
			<table class="table table-hover">
			<!-- ユーザーのカートアイテムを表示させる -->
				<thead>
					<tr>
						<th></th>
						<th>商品名</th>
						<th>数量</th>
						<th>価格</th>
						<th>アーティスト</th>
						<th>ジャンル</th>
					</tr>
				</thead>
			 <% @customer.cartitems.each do |o| %>
			 <tbody>
					<tr>
						<td><%= attachment_image_tag o.product, :product_image, :fill, 150, 150, fallback: "no_image.png", size:'100x100' %></td>
						<td><%=o.product.product_name %></td>
						<td><%=o.purchase_quantity %></td>
						<td><%=o.product.product_price %></td>
						<td><%=o.product.artist.artist_name %></td>
						<td><%=o.product.genre.genre_name %></td>
			　　　　  </tr>
			   </tbody>
		    </table>
		<% end %>
	</div>